/*#include<iostream>
#include<string>
#include"SortedList.h"
#include"Car.h"

int main() {
    SortedList<Car> carList(5);

    // Adding cars to the list
    carList[0] = Car(2024, "Chevrolet Corvette", 490);
    carList[1] = Car(2024, "Porsche 718 Boxster", 350);
    carList[2] = Car(2024, "Toyota GR Supra", 253);
    carList[3] = Car(2024, "BMW Z4", 382);
    carList[4] = Car(2024, "Lotus Emira", 360);

    // Sorting the list
    carList.bubbleSort();

    // Displaying the sorted list
    std::cout << "Sorted List of Cars:\n";
    for (int i = 0; i < carList.getSize(); i++) {
        std::cout << carList[i] << std::endl;
    }

    // Generating descriptive tables for Car and SortedList classes
    // Displaying UML diagram for Car and SortedList classes

    return 0;
}*/